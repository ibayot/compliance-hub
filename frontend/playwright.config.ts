// Playwright's auto-discovered entry point. The newer suite is the default;
// use playwright.old.config.ts when running the legacy numbered specs.
export { default } from './playwright.new.config';
